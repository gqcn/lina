## 1. 路由合同与产物装载

- [ ] 1.1 定义动态插件路由合同结构，补充固定前缀、治理字段与校验规则
- [ ] 1.2 在构建链路中从`backend/api/**/*.go`提取`g.Meta`路由元数据，并嵌入运行时产物
- [ ] 1.3 在宿主装载链路中解析动态路由合同并回填到`manifest.Routes`

## 2. 宿主分发与治理骨架

- [ ] 2.1 实现`/api/v1/extensions/{pluginId}/...`统一分发入口，仅让固定前缀请求进入动态插件链路
- [ ] 2.2 实现宿主侧动态路由匹配、登录校验、权限校验和登录态上下文注入
- [ ] 2.3 对当前未接入执行器的动态路由返回明确的`501`占位响应
- [ ] 2.4 抽象动态路由执行器接口、请求／响应快照骨架与稳定`v1` bridge envelope，先由默认占位执行器承接
- [ ] 2.5 将动态路由匹配结果绑定到当前`active release`运行时快照，并按`runtimeKind`选择执行器
- [ ] 2.6 在运行时产物中嵌入动态路由 bridge ABI 合同，固定未来执行入口与编解码约定

## 3. 权限与文档投影

- [ ] 3.1 基于动态路由`permission`声明自动生成隐藏合成权限菜单，并复用`sys_menu.perms`
- [ ] 3.2 在插件启用、禁用、卸载时联动同步合成权限菜单
- [ ] 3.3 将已启用动态插件的路由合同投影到宿主`OpenAPI`，展示固定公开路径

## 4. 样例与测试

- [ ] 4.1 更新`plugin-demo-dynamic`样例路由元数据，覆盖最小治理字段
- [ ] 4.2 补充后端测试，覆盖路由合同提取、产物装载、路径匹配、权限校验规则与合成权限菜单
- [ ] 4.3 补充构建器测试，覆盖动态路由合同随运行时产物输出

## 5. 真实执行阶段

- [ ] 5.1 补齐真实`Wasm`动态路由执行器，并将当前快照骨架接到实际运行时桥接
- [ ] 5.2 在执行器接入后补齐插件本地中间件执行链与真实响应回写
- [ ] 5.3 将`OpenAPI`投影升级为“可执行路由展示`200/500`，占位路由展示`501`”的运行时感知模型
- [ ] 5.4 更新`hack/tests/e2e/plugin/TC0067-runtime-wasm-lifecycle.ts`，补充`TC-67j`验证动态路由返回真实`Wasm bridge`响应

## Feedback

- [ ] **FB-1**：移除`apps/lina-core/internal/service/plugin`中的编译阶段实现与调用逻辑，统一收敛到`hack/build-wasm`
